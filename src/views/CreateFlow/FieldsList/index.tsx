import React from 'react';

import { FlowField } from 'types/Flow';

export interface FieldsListProps {
  fields: FlowField[];
}

const FieldsList: React.FC<FieldsListProps> = (props) => {
  return (
    <div>
    </div>
  );
};

export default FieldsList;
